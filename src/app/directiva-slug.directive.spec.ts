import { DirectivaSlugDirective } from './directiva-slug.directive';

describe('DirectivaSlugDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaSlugDirective();
    expect(directive).toBeTruthy();
  });
});
